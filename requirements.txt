stable-baselines3[extra]>=2.3.0
gymnasium>=0.29.0
numpy>=1.26.0
pyyaml>=6.0
tensorboard>=2.16.0
torch>=2.2.0

# LeRobot is used as an installed library (dataset format + ACT/SmolVLA
# policies + lerobot-train CLI) by collect_data.py / train_act.py /
# run_policy_act.py. Install from a local checkout:
#   pip install -e ../lerobot
# or pin a released version once this pipeline is stable:
# lerobot>=0.x.x
