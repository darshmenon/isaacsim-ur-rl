stable-baselines3[extra]>=2.3.0
gymnasium>=0.29.0
numpy>=1.26.0
pyyaml>=6.0
tensorboard>=2.16.0
torch>=2.2.0

# --- LeRobot: needs a SEPARATE venv, not this one -------------------------
# Isaac Sim requires Python 3.10 (this repo's .venv); lerobot requires
# Python >=3.12 and pins torch<2.12 (conflicts with the torch this venv
# already has) -- the two cannot coexist in one interpreter/venv.
# collect_data.py (this .venv, py3.10) writes raw episodes to disk;
# package_dataset.py / train_act.py / run_policy_act.py's policy loading
# (a separate .venv312, py3.12) read/train/eval on them. One-time setup:
#   virtualenv -p python3.12 .venv312   # stdlib `venv` needs python3.12-venv
#                                        # (ensurepip) which may not be
#                                        # installed; virtualenv sidesteps it
#   .venv312/bin/pip install -e "../lerobot[dataset,training]"
#
# --- OpenPI π₀ (third venv under /home/asimov/openpi/.venv) ---------------
# train_pi0.py launches OpenPI's scripts/train.py with TrainConfig
# pi0_ur10_reach_lora. Already installed via:
#   cd ~/openpi && GIT_LFS_SKIP_SMUDGE=1 uv sync && GIT_LFS_SKIP_SMUDGE=1 uv pip install -e .
#   python train_pi0.py --dry-run
# ----------------------------------------------------------------------
